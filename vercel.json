{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install && npm --prefix apps/frontend install",
  "outputDirectory": "apps/frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
